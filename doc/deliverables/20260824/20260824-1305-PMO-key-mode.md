---
角色: 项目总控
任务ID: 20260824-1305-PMO
触发阶段: 变更评估
依赖输入: 20260824-1235-PMO
输出时间: 2026-08-24 13:05
替代版本: 20260824-1235-PMO（仅替代 Key 配置方式结论）
---

# 高德 Key 配置方式调整记录

## 结论区

- 保留 `ea308f0` 中酒店高德迁移、异常 fallback、中文城市映射、ranking Top-K、稳定排序、近期 POI 抑制和固定 mock 测试。
- 地点和餐饮恢复为 `0def779` 中的代码内 Key 常量用法。
- 酒店新增同类代码内 `AMAP_KEY` 常量，不再读取环境变量。
- 删除不再使用的 `.env.example`；测试通过 monkeypatch 临时替换酒店 Key，仍不访问真实外网。

## 验证结果

- `python -m pytest -q`：9 passed。
- `python -m compileall -q backend scripts/getdata tests`：通过。
- `git diff --check`：通过。
- 环境变量读取、缺失 Key 分支和辅助函数已从三类采集脚本移除。

## 待确认区

- P1：推送前是否执行一次真实高德 smoke test。
- P2：仓库未来若转为公开或增加更多成员，是否恢复环境变量/Secret 管理。

## 风险与约束区

- Key 会随 Git 历史分享给所有仓库读取者；这是本轮用户明确接受的便利性取舍。
- 所有协作者共享调用配额，任一成员的高频调用会影响另一成员。
- Key 失效或配额耗尽时，地点和餐饮采集会失败；酒店仍可回退模拟数据。

## 变更说明

- 变更类型: 技术调整
- 影响范围: 地点、餐饮、酒店鉴权配置与对应测试
- 失效内容: 20260824-1235-PMO 中“Key 通过 `AMAP_API_KEY` 环境变量注入”的结论
- 保留内容: 20260824-1235-PMO 的功能改动、测试结论和 MVP 边界

## 下一步

- 完成一次真实数据 smoke test后推送当前 `main`。
