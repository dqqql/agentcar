---
角色: 项目总控
任务ID: 20260824-1345-PMO
触发阶段: 执行准备
依赖输入: 20260824-1330-PMO
输出时间: 2026-08-24 13:45
---

# 前后端联调前酒店输入同步

## 结论区

- 酒店 CLI 移除输出目录标识后，pipeline 和随机联调脚本已同步从 6 行输入缩减为 5 行。
- 现有前端 `/api` 代理指向 `http://127.0.0.1:8000`，前后端可分别通过 FastAPI/Uvicorn 和 Vite 启动。

## 验证结果

- `python -m pytest -q`：10 passed。
- `python -m compileall -q backend tests`：通过。
- `git diff --check`：通过。
- pipeline 酒店输入结构：城市、半径、数量、入住日期、晚数，共 5 项。

## 待确认区

- P1：需要用户在页面完成一次真实杭州或北京输入的 E2E 验证。

## 风险与约束区

- 页面完整流程会依次访问地点、餐饮和酒店外部接口，耗时和成功率受网络及高德配额影响。
- 前端必须通过 Vite 服务打开，不能直接双击 `index.html`。

## 下一步

- 启动 8000 端口后端和 5173 端口前端，在页面输入明确包含城市、人数、预算和偏好的中文需求。
