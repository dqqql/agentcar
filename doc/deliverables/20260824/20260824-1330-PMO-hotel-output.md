---
角色: 项目总控
任务ID: 20260824-1330-PMO
触发阶段: 变更评估
依赖输入: 20260824-1305-PMO
输出时间: 2026-08-24 13:30
---

# 酒店输出交互与数据检查记录

## 结论区

- “输出目录标识”仅用于拼接生成目录名，不是业务输入，已从交互问题中移除。
- 程序内部固定使用 `candidates`，新目录格式为 `hotel_candidates_入住日期_时间戳`，避免原来的 `hotel_hotel_candidates` 重复命名。
- 杭州本次输出成功：JSON 和 CSV 均为 10 条记录，10 个唯一 ID、10 个唯一名称，全部来源为 `amap_place_around`。
- 10 条记录均在 4000 米搜索半径内；经纬度、日期和价格区间检查均无异常；核心字段无缺失。

## 数据口径

- 真实高德字段：POI 名称、地址、坐标、电话（若高德返回）、高德 POI ID。
- 派生字段：中心点距离。
- 原型模拟字段：评分、评论数、设施、房型、房量、库存状态和价格。
- 因此本次结果可证明高德酒店 POI 采集成功，但不能证明房价、房态或评分是实时真实数据。

## 验证结果

- `python -m pytest -q`：10 passed。
- `python -m compileall -q scripts/getdata/hotel tests`：通过。
- `git diff --check`：通过。
- 新增测试确认 `prompt_user_inputs()` 只读取 5 个业务输入，不再读取输出标签。

## 待确认区

- P1：后续页面是否需要明确标记“原型模拟价格/房态”。
- P2：是否接入真实酒店价格与库存供应商；当前 MVP 不阻塞。

## 风险与约束区

- `source.provider=amap_place_around` 表示 POI 来源真实，不代表记录中所有业务字段都来自高德。
- 若直接向用户展示模拟价格、评分和库存，应增加原型数据标记，避免误解。

## 变更说明

- 变更类型: 轻微交互调整 + 数据验证
- 影响范围: 酒店 CLI 输入与输出目录命名
- 失效内容: 用户需要填写“输出目录标识”的旧交互
- 保留内容: 酒店数据结构、保存格式、高德采集和 fallback 逻辑

## 下一步

- 重新运行酒店脚本，确认第五个问题“入住晚数”后直接开始抓取。
