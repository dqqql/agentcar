---
角色: 项目总控
任务ID: 20260824-1235-PMO
触发阶段: 执行准备
依赖输入: 20260824-1025-PMO, 20260824-1222-QA
输出时间: 2026-08-24 12:35
---

# 推荐改动抽取、测试与合并记录

## 结论区

- 已按对比文档推荐顺序迁移酒店数据源、抽取 ranking 有效改动、建立固定 mock 测试并完成回归。
- 酒店脚本已从 Nominatim/Overpass 迁移到高德地理编码与周边搜索；`AMAP_API_KEY` 缺失、请求失败或结果为空时可使用本地城市坐标与模拟酒店回退。
- 地点、餐饮、酒店脚本中的明文高德 key 已迁移到环境变量；仓库密钥扫描未再发现原 key。
- ranking 保留 `RankingService`、`RankingRequest`、`RankingResult` 和 pipeline 契约，只增加可配置 Top-K、稳定同分排序及近期 POI 抑制；未把规则模拟描述为训练好的序列模型。
- 新增 9 个固定 mock 测试，覆盖酒店密钥/归一化/fallback/adapter、ranking 契约/Top-K/偏好/历史，以及 pipeline 中文城市映射。

## 验证结果

- `python -m pytest -q`：9 passed。
- 后端 smoke：`/`、`/health`、`/openapi.json` 均通过；根接口 ranking/frontend 状态已同步为 ready。
- 前端 `npm run build`：通过。
- `python -m compileall -q backend scripts/getdata tests`：通过。
- 酒店无 key 函数级 smoke：生成非空 `local_fallback` 酒店记录并可适配为 `CandidatePoi`。
- 明文 key 扫描：未发现对比目录中泄漏的 key 或地点脚本旧 key。

## 待确认区

- P1：旧高德 key 必须由项目所有者在高德控制台完成轮换；代码仓库无法代替服务端操作。
- P1：使用轮换后的 key 做一次真实北京数据 smoke test，验证配额、字段质量和真实链路耗时。
- P2：是否将固定 mock 测试接入 CI。

## 风险与约束区

- 真实高德接口受网络、配额和数据质量影响；本轮自动化测试刻意不访问外网。
- 当前历史输入只有 POI ID，因此仅实现近期访问抑制，不实现历史标签重叠或 Bi-LSTM。
- ranking 仍是三类候选分组排序，不是完整的空间/时间路线组合算法。
- Windows 控制台的交互式中文 stdin 可能受编码影响；pipeline 使用现有 locale 编码逻辑，函数级业务路径已验证。

## 依赖关系

- 依赖 项目总控 的 20260824-1025-PMO：采用其四步推荐合并顺序。
- 依赖 测试经理 的 20260824-1222-QA：按 P0 fallback、契约和密钥风险建立门禁。

## 变更说明

- 变更类型: 技术调整 + 测试补齐 + 文档同步
- 影响范围: 酒店数据采集、地点/餐饮密钥配置、ranking、pipeline 城市映射、后端状态口径、测试与开发文档
- 失效内容: 酒店依赖 Overpass、仓库无固定 mock 测试、ranking/frontend 状态为 planned 的旧结论
- 保留内容: MVP 文本优先、候选分组排序、复杂路线优化和训练模型后置

## 下一步

- 使用轮换后的 `AMAP_API_KEY` 执行一次真实 E2E smoke。
- 按需把 `python -m pytest -q` 加入 CI。
