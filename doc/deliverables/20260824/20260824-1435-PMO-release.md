---
角色: 项目总控
任务ID: 20260824-1435-PMO
触发阶段: 执行准备
依赖输入: 20260824-1400-PMO
输出时间: 2026-08-24 14:35
---

# 文档、忽略规则与推送前检查

## 结论区

- README 已同步一键启动、页面测试文本、酒店五项输入、自动目录命名、10 项测试和真实/模拟字段边界。
- `doc/流程图版开发进度.md` 已更新到酒店高德迁移、ranking Top-K、固定 mock 测试、一键启动和杭州真实 POI smoke 的当前状态。
- `.gitignore` 已覆盖 Python/前端缓存、构建产物、运行日志、根测试输出、后端临时音频、覆盖率文件和三类数据输出。
- `doc/deliverables/` 已正式解除忽略，后续交付文档无需 `git add -f`。
- 历史误跟踪的 ASR 临时音频已从 Git 索引移除，本地文件保留。

## 验证结果

- `git fetch github main`：远程无新增提交，本地可直接快进推送。
- `python -m pytest -q`：10 passed。
- `python -m compileall -q backend scripts/getdata tests`：通过。
- 前端 `npm run build`：通过。
- `git diff --check`：通过。
- 活跃文档旧口径扫描：未发现“9 个测试”“环境变量 Key”“ranking 未接入”等过期描述。
- 跟踪文件生成物扫描：仅保留 `tests/output/.gitkeep`，无缓存、日志、构建产物或临时音频。

## 待确认区

- P1：推送后由 GitHub Actions 将 `main` 同步到 Gitee；若工作流失败需检查仓库 Secrets。
- P1：完整页面真实 E2E 仍需用户在一键启动页面中输入固定杭州文本确认。

## 风险与约束区

- 高德 Key 按当前双人协作决定保存在代码中，推送后仓库读取者均可见并共享配额。
- 酒店价格、评分、库存等字段仍是原型模拟数据，README 和进度文档已明确标记。
- GitHub 到 Gitee 工作流使用强制镜像推送；本次只直接推送跟踪分支 `github/main`。

## 变更说明

- 变更类型: 文档同步 + 仓库清理 + 发布前验证
- 影响范围: README、进度文档、交付文档、忽略规则和历史临时文件跟踪状态
- 失效内容: 9 项测试、Overpass 当前失败、一键启动未实现等旧状态
- 保留内容: 历史交付文档作为决策追溯，不改写当时结论

## 下一步

- 提交本轮文档与仓库清理，并推送 `github/main`。
